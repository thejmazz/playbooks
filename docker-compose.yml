version: '2'

services:
  ansible:
    build: .
    working_dir: /root
    network_mode: host
    volumes:
      - ./hosts:/etc/ansible/hosts
      - ./plays:/root/plays
      - $SSH_AUTH_SOCK:/ssh-agent
    environment:
      - SSH_AUTH_SOCK=/ssh-agent

